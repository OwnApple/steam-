# 课程设计需求说明书：基于Steam游戏推荐系统的设计与实现

## 1. 项目概述
- **项目名称**：基于Steam数据的游戏推荐系统设计与实现
- **课程名称**：《推荐系统算法实践》(SK3016)
- **要求归类**：设计性实验（8学时）
- **项目目标**：理解推荐系统的架构与算法思想，并利用Python等相关技术栈设计并实现一个完整的基于Steam游戏和用户交互数据的推荐系统Web应用。

## 2. 核心功能指标 (KPI)
根据课程设计要求，本系统需实现以下核心功能模块：

1. **物品推荐 (核心业务)**：向不同用户展示个性化的游戏推荐列表。系统中**必须包含两个或两个以上的推荐算法**（结合已有数据源，可采用如：DSSM双塔模型、基于物品的协同过滤、基于用户的协同过滤、基于标签内容的推荐等）。
2. **用户交互日志记录**：追踪用户对游戏物品的交互行为（例如浏览、点击、评分、评论等），并将这些行为日志持久化存储在数据库中。
3. **用户日志流转与动态更新**：具备能够处理用户的最新交互数据的能力，使得推荐列表随着用户的交互行为（及时间推移）发生动态更新和重排。
4. **账号体系 (登录/注册)**：拥有注册功能，未注册用户需先完成注册；认证通过后的账号登录系统即可获取个性化界面。
5. **用户中心管理**：提供用户个人信息（属性特征）的维护修改功能，以及个人历史交互（评分、购买）记录的查询功能。
6. **友好的人机交互界面**：要求界面友好，具有较强的容错性（如输入错误判断过滤），同时程序的输出逻辑说明应直观清晰。

## 3. 数据说明与资产
项目主要源于Steam相关评论及游戏信息，需在此基础上做分析、推荐模型训练及系统应用展示。
- **原始数据源**：`明末评论.xls` 等初始信息资料。
- **现行业务数据集 (`data/` 目录)**：
  - `users_cleaned.csv`：用户基础属性或特征库清洗结果。
  - `games_cleaned.csv`：游戏物料特征（标题、特征标签等）。
  - `ratings_cleaned.csv`：历史用户打分/交互流水日志。
  - `full_features_for_dssm.csv`：已整理好包含游戏特征与用户特征的宽表数据，可直接用于深度学习双塔模型训练等算法推演。

## 4. 推荐系统标准处理流与进阶要求
在开发与算法代码层面，应显式或隐式呈现以下工作流环节：
- **数据预处理与收集**：数据清洗、预处理及数据转换。
- **特征工程 (Feature Engineering)**：从数据源中抽取用户特征向量及打散游戏信息特征（如有内容或标签，进行独热编码等预处理）。
- **模型持久化与调用**：对于耗时较长的深度学习类模型（如DSSM模型），应提前训练保存为 `.keras` 等本地文件。在Web应用启动时读取预训练权重直接对推荐结果进行推理评分，以避免应用中发生模型阻断等待，也可通过定时脚本实现模型热更替。
- **推荐展示与闭环反馈**：系统能够基于特定用户 ID 获取推荐。并在前端页面内加入交互埋点（如打分评价、浏览点击表单）。相关数据使用 `POST` 存入数据库，结合实时的“推荐->交互->数据库打表->查询计算相似物品(ItemCF等)->再次下发”从而实现闭环个性化重排。
- **数据库查询速度优化 (加分项)**：当数据量膨胀时，需针对读取瓶颈设立 MySQL 索引。例如针对推荐记录表使用 `CREATE INDEX idx_user_id ON recommendations(user_id);`，从而极大降低带 `WHERE` 的 `SELECT` 耗时。

## 5. 项目验收与考核形式
- **产出内容**：能够稳定运行的**推荐系统Web应用源代码**，以及 **>3分钟的系统演示、算法逻辑讲解录屏视频**。
- **代码规范考核**：关注代码缩进、空行、注释详略、可扩展的工程骨架与异常判断等编程素养指标。
- **提交与申报**：个人或团队（1~6人）完成工作分解，源码通过FTP提交；并在指定在线文档系统完成项目和成员的信息填报，用于最终评分依据。
- **云端部署与上线验证 (进阶加分项)**：建议在答辩时将项目挂载部署至阿里云或腾讯云轻量级 Ubuntu 服务器上。需配合创建 Python 虚拟环境 (venv)、挂载 MySQL 服务端口开发及使用 `app.run(host='0.0.0.0', port=5000)` 让公网评审浏览。（如服务器性能如 2C2G 遇内存瓶颈报错 `Killed`，需做数据量删减或不加载大模型进行妥协验证）。
